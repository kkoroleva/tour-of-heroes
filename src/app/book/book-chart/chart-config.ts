export interface ChartConfig {
  type: string;
  data: Object;
  options: Object;
};

export interface ChartConfigData {
  type: string;
  data: Object;
  options: Object;
};

export interface ChartConfigOptions {
  type: string;
  data: Object;
  options: Object;
};

